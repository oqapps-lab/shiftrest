node_modules/
.expo/
.expo-shared/
dist/
web-build/

# Platform (Expo prebuild outputs — never commit)
ios/
android/

# Env
.env
.env.local
.env.*.local
.env.production

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
*.orig.*
*.jsbundle
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Supabase CLI working dir
supabase/.temp/

AF4951D7-*

# Code-signing credentials — passwords + certs
# (paranoid: *.p12 / *.mobileprovision already match cert+profile, but
#  credentials.json itself contains the .p12 password in plaintext)
credentials.json
credentials/

# Project-local Claude state (scheduled task locks, etc)
.claude/

# Untracked working notes / ASO drafts
aso/


# macOS resource forks (AppleDouble) + thumbnail metadata
._*
.DS_Store
